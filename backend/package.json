{
  "name": "ccm-backend-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers API Gateway and Domain Workers for Calibration Commercial Module",
  "main": "gateway/index.ts",
  "scripts": {
    "dev": "wrangler dev gateway/index.ts --port 8787",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "@supabase/supabase-js": "^2.45.4",
    "hono": "^4.6.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.80.4"
  }
}
